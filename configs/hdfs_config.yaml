# HDFS Dataset Configuration
dataset:
  name: "HDFS"
  mode: "load"
  output_dir: "results/hdfs"

loading:
  source_dir: "data/hdfs/preprocessed"
  num_templates: 29
  # List of files to copy to the output directory
  files:
    - "HDFS.npz"
    - "HDFS.log_templates.csv"
    - "anomaly_label.csv"
    - "vocabulary.pkl"
    - "vocab.json"
    - "Event_occurrence_matrix.csv"

# Sections below are ignored in 'load' mode, but kept for reference
parsing: {}
sequencing: {}
features: {}
output: {}